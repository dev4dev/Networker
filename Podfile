# Uncomment the next line to define a global platform for your project
platform :ios, '11.0'

def pods 
  pod 'Alamofire'
  pod 'RxSwift'
  pod 'RxCocoa'
  pod 'ObjectMapper'
end

['Networker', 'Demo'].each do |name|
  
  target name do
    use_frameworks!
      
    pods()
  end
  
end